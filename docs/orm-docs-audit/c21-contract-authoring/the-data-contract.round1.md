I read the page once, as a Prisma 7 user would. Findings below.

File: `/Users/wmadden/Projects/prisma/web/.claude/worktrees/error-reference-follow-ups-81a8e7/apps/docs/content/docs/orm/contract-authoring/the-data-contract.mdx`

## Sentences I could not restate after one reading

1. Line 31: "Completion covers field, model, and block attribute names and their argument keys, taken from the installed target and extensions."
   - Stopped by: "the installed target" (a target of what? I have no idea what a target is — the word appears here and nowhere else on the page) and "extensions" (first use, undefined). "block attribute names and their argument keys" is three nested nouns.
   - Plainer: "Autocomplete suggests attribute names and their arguments, based on the database package and extension packages you have installed."

2. Line 49: "The contract also identifies the exact version of the schema it describes."
   - Stopped by: the note at line 39 just told me schema means the database's actual structure. So this says the contract identifies a version of my live database, which cannot be right. Same clash at line 96 ("hashes that identify this exact version of the schema") and line 78 ("plan schema changes"). After one reading I cannot tell which meaning of "schema" is in force in any given sentence.
   - Plainer: "The contract also carries a hash that identifies this exact version of the contract."

3. Line 49: "`prisma contract emit` computes hashes over its contents."
   - Stopped by: "its" — the source file's contents, or the emitted files' contents? And what do I do with a hash?

4. Line 49: "When you run [`prisma db sign`](/cli/db-sign), Prisma ORM records which contract the database matches in a small table it creates in the database."
   - Stopped by: too much at once, and no answer to when I run this. After a migration? Before deploy? Once ever? Also "a small table" — named what? It will show up in my database and I will want to know.

5. Line 86: "Keep the other form out of the project, or treat it as a generated reference, so the two can never disagree."
   - Stopped by: "the other form", "a generated reference". Generated by what? I do not know what action I am being told to take or avoid.

6. Line 94: "named types, enums, and `type` blocks, each a structured value stored inside its parent row with no table of its own"
   - Stopped by: "each" — does it apply to enums too? An enum is not a structured value. And I do not know the difference between "named types" and "`type` blocks"; they read as the same thing listed twice.

7. Line 77: "`db` reads `contract.json` and compares its hashes with the record `prisma db sign` wrote in the database."
   - Stopped by: bare `db`. Is that a CLI command group, a runtime object, a package? The other two bullets name real things (`contract.d.ts`, "the migration tooling"); this one names a two-letter thing I have never seen.

## Words and phrases I had to guess

- "the installed target" (line 31) — guessed: the database-specific package, maybe `@prisma/orm-postgres`.
- "extensions" (line 31) vs "extension packs" (line 95) vs "extension-provided types" (line 66) — guessed these are all the same thing. The page uses three names for it and defines none.
- "machine-readable files" (line 33) — guessed `contract.json` and `contract.d.ts`, confirmed 14 lines later. Name them where you first mention them.
- "which database features the contract needs" (line 47) — guessed things like JSON columns or pgvector.
- "The query APIs" (line 76) — guessed this is the new name for what I call PrismaClient. Plural, so possibly several.
- "contract source" (line 47) vs "contract source" declared in config (line 53) — guessed both mean the `.prisma` or `.ts` file I write.
- "`prisma contract infer`" (line 82) — guessed it reads an existing database and writes a contract for it. The page never says.
- "storage details" (line 93) — guessed: the database-level objects.

## Places I asked "so what do I actually type?" and got no answer

1. **I have a `schema.prisma` today.** Line 9 says `contract.prisma` "replaced `schema.prisma`". The page never tells me how to move from one to the other. Is there a command? Do I rename the file and add `// use prisma-8`? This is the single biggest gap for anyone coming from 7.
2. **Installing.** `@prisma/orm-postgres` appears in the config snippet with no install command. No `npm install` anywhere on the page.
3. **`prisma generate` is gone.** I ran it after every schema change for two years. Line 68 says run `prisma contract emit` instead, but never says `generate` is retired, so I am left wondering whether I now run both.
4. **Does anything run `emit` for me?** Line 68 says "after every change". Does `prisma migrate dev` do it? Is there a watch mode? Not said.
5. **When to run `prisma db sign`.** Mentioned twice, never placed in a workflow.
6. **Where `contract.d.ts` gets consumed.** Line 76 says the query APIs load it. No import line, no client construction, nowhere on the page.
7. **"For the cases PSL does not cover" (line 84).** Which cases? I cannot decide between the two authoring modes without knowing. And line 86 then says "You give up nothing by staying with PSL", which directly contradicts line 84.
8. **TypeScript mode config.** Line 66 says the extension selects the mode. Do I just point `contract:` at `./prisma/contract.ts`? Probably, but the page only shows the `.prisma` case.

## Places that explain the insides when I wanted instructions

1. Line 31, the whole language-server paragraph. I asked "what goes at the top of my file". I got diagnostics, completion internals, argument keys, and the `// use prisma-next` history from a release candidate I never used. One sentence would do: put `// use prisma-8` on the first line; editor support depends on it.
2. Line 49: "The client reads that record before its first query and logs a warning if the record does not match the contract the client holds." That is the client's startup sequence. What I need is: my app warns when the database is out of date; use `prisma db verify` in CI to fail instead.
3. Line 47: "The same source always produces the same two files, so you can read them in a code review, hash them, and hand them to tools and coding agents." This is a design rationale. Fine in "Why a contract", but it sits where I am still trying to learn what the files are.

## Could I do what the page is for, after one reading?

Partly. If I were starting a brand-new project I could write a `contract.prisma`, put `// use prisma-8` at the top, point `prisma.config.ts` at it, and run `npx prisma contract emit`. That much is clear.

What I would still not know:

- How to convert my existing `schema.prisma` to a contract. The page announces the replacement and then never addresses the upgrade.
- What to install, or what `@prisma/orm-postgres` is.
- How to query. I never see a client imported or constructed, and "the query APIs" is not a thing I can type.
- Whether `prisma generate` still exists.
- When to run `prisma db sign`, and what table it creates.
- Which situations require the TypeScript builder, given the page both names such cases and says I give up nothing by avoiding it.
- Whether "schema" means my database or my contract, in any sentence after the note at line 39.