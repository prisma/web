Both pages check clean and the line counts are unchanged.

## Corrections made

| Page / line | Claim | Deciding source | Verdict | Now says |
|---|---|---|---|---|
| data-contract 50 | `import { definePrismaConfig } from "prisma/config"` matches what `prisma orm init` writes | `wip/prisma-src/packages/1-framework/3-tooling/cli/src/commands/init/templates/code-templates.ts:configFile` | Corrected | `from "@prisma/cli-engine"`. The string `prisma/config` appears nowhere in the source tree; `@prisma/cli-engine` is a real pinned dependency (`.../cli/package.json:55`) and is what the template and `examples/prisma-8-demo/prisma.config.ts` both use. |
| data-contract 77 | `migration plan` "works out what the database needs" | `.../cli/README.md` §`prisma migration plan` ("No database connection is needed — fully offline") | Corrected | "compares your contract against the last one and writes a migration" |
| artifact 16 | "Each file starts with a comment saying they are generated" | `.../emitter/src/emit.ts:38-45`; `examples/prisma-8-demo/src/prisma/contract.json:1287` | Corrected | `contract.d.ts` starts with the comment; `contract.json` carries the notice in a `_generated` entry appended at the end. |
| artifact 98 | `roots`: "one entry per model" | `.../contract-ts/src/build-contract.ts:952` (`if (!semanticModel.sharesBaseTable)`) | Corrected | "one entry per table that stores a model". Variants sharing a base model's table contribute no root — confirmed in the demo file, where `Bug` and `Feature` are absent from `roots`. |
| artifact 107, 115 | "The hash is part of the type name, so a `contract.d.ts` from one run does not type-check against a `contract.json` from another" / "TypeScript catches it" | `.../postgres/src/runtime/postgres.ts:102` — `readonly contractJson: unknown` | Corrected | The runtime option is typed `unknown`, so TypeScript never compares the two files. Bullet now just says each type carries the hash of that part of the contract; the closing sentence now points at the CI check already on the page. |
| artifact 112 | `contract.d.ts` excerpt used double quotes | `examples/prisma-8-demo/src/prisma/contract.d.ts:41`; emitter formats with `singleQuote: true` | Corrected | single quotes |

## Confirmed (no change)

`orm init` writes the contract, `prisma.config.ts`, `src/prisma/db.ts`, and `.env.example` (`init-scaffold.ts:42-59`). `// use prisma-8` written into both PSL starters; the parser and interpreter never read it; the language server behind the VS Code extension gates diagnostics on it (`language-server/src/document-diagnostics.ts:27`, `schema-directive.ts:1`). `contract.ts` templates carry no header. Three packages `@prisma/orm-postgres` / `-sqlite` / `-mongo` (`packages/9-public/@prisma/`); `./config`, `./runtime`, `./contract-builder` all in the exports maps. File extension picks the authoring mode and the output path is derived from the contract path (`postgres/src/config/define-config.ts:28-51`), so the config names exactly one file. `db migrate` writes the marker (`operations/migrate.ts`), `db init` does too (CLI README §`prisma db init`), `db sign` verifies the schema first then signs (`cli/src/orm/db/sign.ts`), `db verify` reports existence and match (README:677). `verifyMarker` is `'onFirstUse' | false` (`runtime-spi.ts:42`), defaults to `'onFirstUse'`, checks before the first query, and only calls `log.warn` with `CONTRACT.MARKER_MISSING` / `CONTRACT.MARKER_MISMATCH` — it cannot be made to throw (`sql-runtime.ts:897-928`). `storageHash` covers tables, columns, keys, indexes, foreign keys, checks, and enum values (`valueSet` entries live in `storage`); `profileHash` covers target and family (`contract/src/hashing.ts:98`). `contract.json` key order and every shape in the sample match the real emitted file. `Contract`, `StorageHash`, `ProfileHash`, and `<Name>Output` / `<Name>Input` per `type` block all present in the emitted `contract.d.ts`. Capabilities merged from the target pack plus `extensions: [...]` (`build-contract.ts:1578-1586`) and checked at query build time (`sql-builder/.../builder-base.ts:62`, `ORM.CAPABILITY_MISSING`). `mongo<Contract>(...)` from `@prisma/orm-mongo/runtime`. `prisma generate` does not exist in the command tree (`cli/src/orm/cli.ts:96-120`). `npx prisma skills sync` exists (`skills/README.md:23`). All 27 link targets resolve, and all seven anchors exist in the target pages.

## Q — unverifiable from this source

1. `npm create prisma@latest -- my-app`, and that it writes `prisma.config.ts` / the contract / `db.ts` and installs the database package, and that its projects ship the skills. `create-prisma` is not in this repo. Searches: `grep -rn "create prisma@latest" packages skills docs examples`, `grep -rn "create prisma" ... --include='*.json'` — both empty.
2. "Delete the `.d` and the import stops resolving." The template writes `'./contract.d'` (`code-templates.ts:dbFile`) and `skills/prisma-8/references/runtime.md:45` repeats it, but nothing states what happens without it. Searches: `grep -rn "contract.d'" packages skills docs`, `grep -rn "\.d'" skills/prisma-8/references/*.md`.
3. "byte for byte on any machine". The emitter README calls the two files deterministic and excludes `_generated` from canonicalization to keep it so (`emitter/README.md:9,109,169`), but there is no cross-machine test. Left as is.

## Notes

- `codecId`, `nativeType`, and `capabilities` still appear, but only as JSON keys in code blocks and inline code, which `page-conventions.md` allows. `#deterministic-emission` and `#keep-the-contract-file-pure` are pinned anchors and must stay.
- No test covers the `// use prisma-8` header being optional for the CLI; that fact comes from the absence of any reference to it in the parser and interpreter.
- Line counts: `the-data-contract.mdx` 142 before, 142 after. `the-contract-artifact.mdx` 158 before, 158 after.