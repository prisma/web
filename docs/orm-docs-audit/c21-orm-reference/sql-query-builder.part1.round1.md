## Sentences I could not restate after one reading

**Line 11** — "Where a method works only on some databases, or has a caveat that only shows up when the query runs, the Remarks call that out."
"the Remarks" arrives capitalised, as if I already know it. I don't meet a Remarks section until line 337. Plainer: "Some methods only work on some databases, or fail only when the query actually runs. Each method's **Remarks** section says so."

**Line 131** — "You build queries from the client's `sql` property, which knows your contract and how your queries are run."
I do not know what "your contract" is. In Prisma 7 I had a schema and a generated client. The word never gets defined, but `contract.json` and `contract.d` show up in the very next code block as things I am supposed to already have. Plainer: say what a contract is and which command produces those two files, or link to the page that does.

**Line 135** — "Create the client with `postgres(...)`, take a runtime from `db.runtime()`, and build queries off `db.sql.public`. On PostgreSQL `db.runtime()` is synchronous, so there is nothing to await."
Three new nouns in one breath: client, runtime, and `public`. And the second sentence implies runtimes are sometimes asynchronous without saying when or what I'd do then. I could not restate what a "runtime" *is* — only that I need one to run a query.

**Line 149** — "`db.sql` holds the result of an internal `sql({ context, rawCodecInferer })` call, connected to the client's way of running queries and to the adapter's rule for how values are stored and read."
This defeated me. "internal", "context", "rawCodecInferer", "adapter", "rule for how values are stored and read" — five unknowns, one sentence, and it describes the inside of the tool. I wanted to know what to type.

**Line 149, second half** — "a `rawCodecInferer` (a fallback rule for how to store and read raw expressions that have no column to take that from)"
"that have no column to take that from" — I read this three times. Take *what* from a column? I think it means: a raw SQL expression has no column, so the builder cannot work out its type, and this supplies the default. If that is the meaning, say that.

**Line 199** — "Each name resolves against the current scope; an unknown name throws an error whose `code` is `ORM.COLUMN_UNKNOWN`."
"the current scope" is never defined. I only work out roughly what it means 140 lines later, from the `lateralJoin` remark about ambiguous names being dropped. Also: does it throw when I call `select()`, when I call `build()`, or when the query runs? The page draws that distinction at line 11 and then never tells me which side this falls on.

**Line 341** — "`.as(...)` returns the wrong shape (a bare join source), which throws `subquery.getRowFields is not a function`."
This is an internal JavaScript error message pasted into a reference page. I can match it if I see it, but "bare join source" versus the join source `.as()` is documented to return at line 528 is a contradiction I cannot resolve. At 528 `.as()` produces exactly what joins want. Here the same value is "the wrong shape". Which is it?

**Line 342** — "the merged scope **drops** the ambiguous names."
I had to guess that "merged scope" means the combined set of column names from both sides of the join. The sentence describes an internal bookkeeping step; what I need is the rule: after a lateral join, if both tables have a column of the same name, you must write `f.post.id`, never `f.id`.

## Words and phrases I had to guess

- **"contract"** — guessed: the new name for the generated schema types. Guessed `contract.json` and `contract.d` are generated by some CLI command.
- **"runtime"** — guessed: the thing that holds the database connection and executes queries.
- **"plan"** (the variable name, used in every example) — guessed: the compiled query object that `build()` hands to `runtime.query()`. The page never once says what `build()` returns or why it is called a plan.
- **"escape hatch"** (line 9) — guessed: a way out of the normal API for cases it doesn't cover.
- **"facet"** — appears only in the anchor `#the-dbsql-facet` (line 133) while the heading says "builder". Two names for one thing; I don't know if a facet is a distinct concept.
- **"function bag `fns`"** (line 187) — guessed: an object full of helper functions. "Bag" is not a word I've seen used this way.
- **"codec"** — never used in prose, but `'pg/text@1'` and `'pg/int4@1'` appear five times and `rawCodecInferer` names it. Guessed: a type tag telling Prisma how to encode and decode a Postgres value.
- **`Uuid`**, **`@@type("pg/text@1")`**, **`@@discriminator(type)`**, **`@@base(Task, "bug")`**, **`types { Embedding1536 = pgvector.Vector(1536) }`** — all new to me since v7. All guessed. The schema is presented as background, but it is the least familiar thing on the page.
- **"adapter"** (lines 149, 339) — guessed: the per-database driver.
- **"join source"** (lines 170, 528) — guessed: anything you're allowed to put on the right of a join.
- **"derived table"** (line 341, 348) — guessed: the SQL term for a subquery used as a table.

## Places I asked "so what do I actually type?"

1. **Where do `contract.json` and `contract.d` come from?** Every example depends on them. No command, no link, nothing. This is the first thing that blocks me from running a single line on the page. "This is a reference page, the project already exists" does not cover it — I have a v7 project and no contract.
2. **SQLite.** Line 14 says SQLite is supported. Every example is `postgres(...)` and `db.sql.public`. What do I import for SQLite? What replaces `public`, given SQLite has no schemas? The page raises SQLite four times (lines 14, 339, 453) and never shows one line of SQLite code.
3. **The codec strings.** `.returns('pg/int4@1')` is required for every raw expression, and there is no list of valid values and no link to one. I can copy `pg/text@1` and `pg/int4@1`. For a `numeric`, a `timestamptz`, a `bool`, I am stuck.
4. **`context`** in the line 154 example. The code is `sql({ context, ... })` with `context` appearing from nowhere. "Pass your `context` (from your database client)" — which client? I just created the client with `postgres(...)`, and that one has `db.sql` already. If this path is for people not using `postgres()`, I have no idea what object to pass.
5. **`limit(n)`** — "`number`, or a numeric expression" (line 491). No example of the expression form. What would I write?
6. **Does `distinctOn()` have to come after `orderBy()`?** The example puts it last (line 477), the prose says "according to the query's `orderBy()`" and "Pair it with an `orderBy()`", but never says whether call order matters. I would have to experiment.
7. **`.as()` on non-SELECT queries.** Line 516 says "a completed `SELECT` query". Is there a way to use an INSERT's returned rows as a source? Silence — possibly answered in part 2.
8. **`orderBy()` twice** — line 396 says calling it again adds secondary keys. Does calling `select()` twice add columns or replace them? Line 226 chains two `select()` calls and the result seems additive, but the prose at line 200 says "Add one computed column" only for the aliased form. For the column-name form at line 199 it says "Keep the named columns" — so a second call presumably replaces. Unclear.

## Places that explain the inside when I wanted the outside

- **Line 149**, the whole `sql({ context, rawCodecInferer })` paragraph. It tells me what `db.sql` *holds internally* and what it is *connected to*. I am reading a reference to write queries. This belongs somewhere else, or reduced to one sentence: "If you are wrapping your own database client instead of using `postgres()`, call `sql(...)` yourself — see X."
- **Line 135** — "On PostgreSQL `db.runtime()` is synchronous, so there is nothing to await." The useful half is "don't await it". The reason is implementation.
- **Line 339 and 453** — "TypeScript types `lateralJoin` as `never`, so the call does not compile". How the restriction is implemented. What I need: "Not available on SQLite; the call won't compile."
- **Line 341** — "`lateralJoin()`'s own first argument already names the derived table" plus the raw `subquery.getRowFields is not a function` error. The instruction "return the chain, don't call `.as()`" is enough.

## A specific problem, not a comprehension one

**Line 390** — "`options.nulls` | `'first' \| 'last'` | No | TypeScript accepts it, but nothing reads it: no `NULLS FIRST` or `NULLS LAST` reaches the SQL, so the database's own null ordering applies."

Then **line 417** gives it a worked example titled "Control direction and null placement", whose comment says `// nulls adds nothing to the SQL`. The page documents a broken option and then demonstrates it. As a reader I assume the option works and I misread the caveat, or that the docs are papering over a bug. Either delete the option from the table and the example, or say plainly "`nulls` is currently ignored; do not use it."

**Lines 135 and 161** are word-for-word identical from "A `select()`, `insert()`..." onward. On second reading I lost my place, thinking I had scrolled backwards.

**Line 19** promises "The select and mutation examples use this `user` / `post` / `post_tag` / `tag` schema". The expanded schema also contains `Address`, `Task`, `Bug`, `Feature`, `user_type`, `Priority` and `Embedding1536`. Nothing in part 1 uses `Address`, `Task`, `Bug` or `Feature`, and they are the most unfamiliar syntax on the page. They made me stop and try to decode `@@base` and `@@discriminator` for no payoff.

## Could I do what the page is for, after one reading?

Partly. I could write a `select` with a `where`, an `innerJoin`, an `orderBy`, a `limit`, and a subquery join — the SQL concepts are ones I know and the examples are concrete and complete. The three-form table for `select()` (line 197) and the join examples are the strongest parts.

What I still would not know:

1. **How to get to line 1.** I cannot produce `contract.json` or `contract.d`, so I cannot run any example. That is a hard stop, and it is the page's own opening code block.
2. **What a "contract" or a "runtime" is**, beyond that I need both.
3. **How to use any of this on SQLite**, despite being told twice it is supported.
4. **Any raw expression whose type isn't text or int4**, because there is no codec list.
5. **When errors surface** — at `build()` or at `runtime.query()`. The page flags the distinction as important and then never applies it to `ORM.COLUMN_UNKNOWN` or `ORM.CAPABILITY_MISSING`.
6. **Whether `select()` accumulates or replaces** when called twice with column names.
7. **Whether the schema's new syntax matters to me.** `Uuid`, `@@type`, `@@discriminator`, `@@base` and the `types { }` block are all new since v7 and all unexplained. I do not know if I must adopt them to use `db.sql`, or if they are incidental to the examples.