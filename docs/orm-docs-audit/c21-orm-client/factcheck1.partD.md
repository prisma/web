# Fact re-check 1, slice D (MongoFieldFilter, dot notation, field update operations, result types)

Line count unchanged (209). Corrections: the library does pass `$regex`, `$elemMatch`, `$size` through `MongoFieldFilter.of` (and the pipeline builder has `$size`/`$regexMatch`), so the "no support anywhere" claim was wrong; `of()` example now uses `'$regex'` on a real field (`age` did not exist); `updateAll()` also takes the field-operations callback and the callback argument is callable for dot paths; `set`/`unset`/`inc`/`mul` no longer "verified" (`mul`/`unset` have unit tests only); `inc`/`mul` are typed on numeric fields only; a misspelled operation is a TypeScript error (closed object type), not `undefined` at runtime; `upsert()` rejects a dot path; the four array operations are tested (`orm/test/collection.test.ts:294-322`, `push`/`pull` also on a live database), so the body no longer says "not tested"; four operations on the same field in one `update()` conflict, so the example is split; the array example moved to `db.orm.users` assuming `tags String[]`; the `ITERATOR_CONSUMED` message wording is now "contains".

Confirmed: eleven `MongoFieldFilter` factories, `isNull` is equals-null, `MongoExistsExpr`, dot path at runtime and the cast, eight field operations and their signatures (`push(value)`, `pull(match)`, `addToSet(value)`, `pop(1 | -1)`), callback typed against the base model after `variant()` so `t.duration.inc(10)` needs `// @ts-expect-error` (slice B's copy without it is wrong), MongoDB `update()` returns `null` only on no match (bad `_id` write throws `ORM.FIELD_IMMUTABLE`), both `components/runtime` import paths, re-await safety, `RUNTIME.ITERATOR_CONSUMED`, aggregate result shapes.

Q: `inc`/`mul` on a missing field (MongoDB server behaviour, not in the tree); the server error text for an array operation on a non-array field (not in the tree). Heading "Array operations (MongoDB, unverified)": "unverified" is no longer true.
