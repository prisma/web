# Fact re-check 1, slice B (Read methods, Custom Collection, Write methods)

693 lines before, 692 after. Corrections: `all()` takes no required argument (it has an optional annotations callback); on PostgreSQL TypeScript refuses all six update/delete methods without `where()`, not just `update()`/`delete()`; the multi-table variant in the page's PostgreSQL schema is `Bug` on `Task`, so the `ORM.OPERATION_UNSUPPORTED` messages for `createAndCount()` and `upsert()` now name those; PostgreSQL `update()` returns `null` whenever the data maps to no columns (a function or an empty object); the MongoDB update input is typed on the base model only, so the `t.duration` examples were replaced with `set()` on base fields; the MongoDB upsert `_id` comes back decoded as a hex string, not an `ObjectId` (codecs.ts, CHANGELOG).

Confirmed: no `count()` read method; `first()` inline filter ANDs with `where()` on PostgreSQL and takes no argument on MongoDB; `orm(...)` with `collections`, `client.connect()`, `client.context`; the `updateAll() requires a .where() filter...` message (code `ORM.WHERE_MISSING`); PostgreSQL create optional fields (nullable, column default, execution default); MongoDB `_id` only optional field; MongoDB create returns passed values plus `_id`; nested `create`/`connect`/`disconnect` PostgreSQL only; MongoDB `updateAll()` three steps; no `db.transaction()` on MongoDB; `deleteAll()` deletes before yielding; `deleteAndCount()` returns 0; `conflictOn` reads only the column name, value unused; PostgreSQL `upsert()` ignores `where()`; omitted `conflictOn` uses the primary key; MongoDB upsert `update` wins over `create` on insert.

Q: the Prisma ORM 7 halves of the diff blocks (no ORM 7 client in the tree); whether the nested `create()` example type-checks without `userId` (matches the source's own doc example).

Operator note: the MongoDB example schema has no numeric field on a base model, so `inc()`/`mul()` cannot be shown on MongoDB without `@ts-expect-error` or a schema change.
