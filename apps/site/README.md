# Prisma Site Host

Primary host app for the multi-zone Next.js setup.

## Run locally

```bash
pnpm install
pnpm dev
```

Runs on `http://localhost:3000`.

## Notes

- This app is the root host zone.
- Content-specific routes are expected to be served by dedicated zones.
