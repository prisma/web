---
title: "Prisma vs Netlify: where should you build your next TypeScript app?"
slug: "prisma-vs-netlify"
date: "2026-09-16"
authors:
  - "Shane Neubauer"
metaTitle: "Prisma vs Netlify for TypeScript apps: which platform?"
metaDescription: "Prisma vs Netlify compared for TypeScript apps: what each platform provisions and checks for you, preview databases, migrations, runtime limits, and pricing."
excerpt: "Prisma catches a service wired to the wrong API or the wrong database before it deploys. Netlify gives every preview a copy of production and runs background jobs for minutes. Here is how to choose."
heroImagePath: "/prisma-vs-netlify/imgs/hero.svg"
heroImageAlt: "Prisma and Netlify logos side by side"
metaImagePath: "/prisma-vs-netlify/imgs/meta.png"
tags:
  - "platform"
  - "prisma-postgres"
  - "orm"
---

If your app is a few TypeScript services that share a Postgres database, **Prisma is the better fit**. You declare the services, their databases, and the calls between them in one TypeScript file. The compiler rejects a service that points at the wrong API or the wrong database schema, and one deploy command creates everything the file describes.

Netlify builds and publishes your site and its functions together. The connections between separate services, and the database each one uses, are yours to set up and keep correct.

**Netlify is the better fit** when your app is mostly pages and assets, or when it runs on Node.js. It is also the better fit if you need background jobs that run for minutes, or if you want every preview to start with a copy of production data.

Prisma's hosting runs on Bun in one region, gives each request 60 seconds to start responding, and gives every preview an *empty* database.

"Prisma" here means more than the ORM:

- [Prisma ORM 8](https://www.prisma.io/docs/orm) is the typed data layer. It is a release candidate, with general availability expected in October 2026, and it runs on any host, including Netlify.
- [Prisma Postgres](https://www.prisma.io/docs/postgres) is the managed database.
- [Prisma Compute](https://www.prisma.io/docs/compute) hosts TypeScript services.
- [Prisma Composer](https://www.prisma.io/docs/composer), in early access, declares how services and databases connect and deploys them to Compute and Prisma Postgres.

This post compares the three hosting products with Netlify. The last section covers running Prisma ORM and Prisma Postgres on Netlify.

## The decision in brief

Choose Prisma if:

- Several TypeScript services call each other, and you want a mismatched API contract or a missing handler to fail the *type check* instead of a deploy or a production request.
- You want every Git branch to be a full environment with its own services and variables. You are willing to create its database with one command or a Composer deploy.
- Your services run on Bun, or can, and one region is enough.
- You accept pre-release tooling. Prisma ORM 8 is a release candidate and Composer is in early access.

Choose Netlify if:

- Your app is mostly pages and assets, and you want a global edge network, caching, and an Image CDN without building them into the app.
- Your code runs on Node.js and you do not want to move it to Bun.
- You have background work that runs for minutes. Netlify Background Functions run up to 15 minutes, and Async Workloads add retries and multi-step jobs.
- You want preview databases that start as a copy of production. Netlify Database creates one for every Deploy Preview.

| | Prisma | Netlify |
| --- | --- | --- |
| Hosting | TypeScript services on Prisma Compute, on Bun, one region each | Serverless and Edge Functions deployed with the site |
| Managed Postgres | Prisma Postgres, usable from any client | Netlify Database, [built on Neon](https://www.netlify.com/blog/netlify-database/) |
| Typed data access | Prisma ORM 8 or any client | Drizzle or any driver or ORM, including Prisma ORM |
| Service-to-service calls | Composer binds each dependency to a typed contract and checks it at compile time | Site and functions share types by import; separate services are wired by hand |
| Previews | One environment per Git branch, deleted with the branch | A Deploy Preview per pull request, plus branch deploys |
| Preview databases | One per branch, created by you or by a Composer stage deploy, no production data | A Neon branch per Deploy Preview with a copy of production |
| Long-running work | 60 seconds to first byte, then `waitUntil`; no WebSockets | Background Functions up to 15 minutes; Async Workloads |
| Billing | Plan fee plus requests, memory, CPU, and bandwidth | Credits for compute, requests, bandwidth, and production deploys |

Sources for the table: the [Compute limitations](https://www.prisma.io/docs/compute/limitations) page and Netlify's [database tooling](https://docs.netlify.com/build/data-and-storage/netlify-database/tooling/), [Background Functions](https://docs.netlify.com/build/functions/background-functions/), and [pricing](https://docs.netlify.com/manage/accounts-and-billing/billing/billing-for-credit-based-plans/credit-based-pricing-plans/) pages.

## What each platform sets up for you

Both platforms deploy from a Git push. They differ in how much of your app's structure they know about.

**On Netlify**, you connect a repository and a build command. Each push builds the frontend and its serverless functions together and publishes them as one site. A page and the function it calls always come from the same commit, and they can share TypeScript types through normal imports.

Netlify has no concept of one deployed service calling another. If your backend grows into several services, you connect them with URLs and environment variables, and you keep their types in sync yourself.

**On Prisma**, Composer works at the level of services:

1. **Declare each service** in TypeScript, together with what it depends on: other services, a Prisma Postgres database, a cron schedule, or object storage.
2. **Give each service a contract**, meaning typed request and response schemas written with arktype, zod, or any Standard Schema validator.
3. **Declare the contracts a service calls** as its dependencies. At runtime the service receives a typed client for each one.
4. **Wire everything in a root module.** TypeScript checks the wiring, so binding a dependency to a service with a different contract, or leaving out a handler the contract requires, is a compile error.
5. **Run `prisma deploy`.** It creates the services on Prisma Compute, the databases on Prisma Postgres, and the authenticated connections between them. Application code never builds a URL from `process.env`.

![What each platform connects: on Prisma, the repository holds the data contract, the service contract, and the root module; prisma deploy provisions the storefront and catalog services on Prisma Compute and a Prisma Postgres database typed by the contract. On Netlify, the repository holds the site and its functions; a git push builds and publishes both, and Netlify Database connects a Postgres branch per preview through NETLIFY_DB_URL.](/prisma-vs-netlify/imgs/what-each-platform-connects.svg)

The two files below declare a storefront that asks a catalog service for a price. The storefront names the catalog's contract in `deps`, and the root module decides which running service satisfies it.

```ts tab="storefront/service.ts"
import nextjs from "@prisma/composer/nextjs";
import { rpc } from "@prisma/composer/service-rpc";
import { compute } from "@prisma/composer-prisma-cloud";
import { catalogContract } from "../catalog/contract.ts";

export default compute({
  name: "storefront",
  deps: { catalog: rpc(catalogContract) }, // [!code highlight]
  build: nextjs({ module: import.meta.url, appDir: ".." }),
});
```

```ts tab="module.ts"
import { module } from "@prisma/composer";
import catalogModule from "./src/catalog/module.ts";
import storefrontService from "./src/storefront/service.ts";

export default module("store", ({ provision }) => {
  const catalog = provision(catalogModule);
  provision(storefrontService, { deps: { catalog: catalog.rpc } }); // [!code highlight]
});
```

Suppose the catalog team changes `getPrice` to return an amount and a currency instead of a single number. Every caller that still expects a number fails to compile. A shared-types package in a monorepo gives you that much.

The root module adds a second check. If it binds `storefront` to a service that serves a different contract, that is a compile error too, not a 404 after the deploy. The [store example](https://github.com/prisma/composer/tree/main/examples/store) in the Composer repository is the complete version of this app.

Databases are declared the same way. A service lists a `postgres()` dependency typed by your **data contract**, the `contract.prisma` schema file that replaced `schema.prisma` in ORM 8. The module that owns the database provisions it. The [deploy applies the database's migrations](https://www.prisma.io/docs/composer/databases) before the service starts, and it refuses to connect a service to a database whose schema does not match the contract the service was compiled against.

You do not need any of this to start. A single service deploys to Prisma Compute from a Git push without Composer, and you can add Composer when a second service or a scheduled job appears.

## Previews and their databases

Both platforms give every branch a running preview. They differ in what sits behind it, and that decides how much setup you do before a preview is useful.

**On Prisma**, a Git branch maps to a [platform branch](https://www.prisma.io/docs/compute/branching) with its own services, environment variables, and any databases created on it. Deleting the Git branch deletes all of that. *The database is not created for you unless you use Composer.* For a single service:

1. Create a database on the branch: `npx prisma@latest postgres create preview-db`.
2. Point the branch at it: `npx prisma@latest project env add DATABASE_URL=<printed-url> --branch feature/search`.
3. Seed it if the feature needs realistic rows, because it starts empty.

A Composer stage deploy, `npx prisma@latest deploy module.ts --stage <branch>`, replaces the first two steps. It creates the databases the module declares and runs their migrations, but the databases still start empty.

A preview uses whatever `DATABASE_URL` it was given, so a preview that inherits the production value *writes to production*. Keep production and preview values in separate scopes, as the [environment variables docs](https://www.prisma.io/docs/compute/environment-variables) describe.

![Every Git branch is its own environment. The main branch runs the storefront and catalog services against the production database, which has id and email. The alice/phone branch runs its own copies against its own database with a phone column, and the bob/avatar branch does the same with an avatarUrl column.](/prisma-vs-netlify/imgs/branch-databases.svg)

**On Netlify**, each Deploy Preview gets a database branch that already holds a copy of production data from when the preview was created. Netlify applies [migration files committed to Git](https://docs.netlify.com/build/data-and-storage/netlify-database/migrations/) on both production and preview deploys, and changes made in the preview branch never reach production.

For testing against production-shaped data, this is the more convenient setup, because there is nothing to seed and nothing to point.

Schema changes travel the same way on both platforms: as migration files in Git that a deploy applies. Prisma ORM 8 helps when two branches change the schema at the same time:

1. Alice adds `phone` on her branch and Bob adds `avatarUrl` on his. Each tests against their own database.
2. Both branches merge. Alice's database has one column, Bob's has the other, and staging and production have neither.
3. In Prisma ORM 6 and 7, migrations ran in timestamp order, so every database had to be walked through the same list. ORM 8 records, for each migration, which schema version it starts from and which it produces, so the migrations form a [graph](https://www.prisma.io/docs/orm/migrations/the-migration-graph).
4. Whoever merged runs `migration plan` once per branch, which writes the two merge migrations.
5. From then on, `db migrate` brings any database from its current version to the merged one.

Two migrations that change the *same* column still need a person to resolve them.

![The migration graph after the merge. State A is production with id and email. Alice's migration adds phone to reach state B, and Bob's migration adds avatarUrl to reach state C. Two merge migrations, one from B and one from C, both reach the merged state D. Each arrow is one migration, recorded as the state it starts from and the state it ends at.](/prisma-vs-netlify/imgs/migration-graph.svg)

The graph belongs to Prisma ORM 8, not to Prisma's hosting, so you get it on Netlify by running ORM 8 there. The hosting adds a database per branch to test against and, with Composer, a deploy that runs the migrations for you.

## Runtime limits

Prisma Compute runs TypeScript HTTP services on **Bun**. Builds detect Next.js, Nuxt, Astro, Hono, NestJS, TanStack Start, and plain Bun servers, and an existing Node.js app has to run under Bun to move. The [limitations page](https://www.prisma.io/docs/compute/limitations) lists the constraints:

- Each service runs in **one region**, and there is no edge runtime.
- **WebSocket servers** are not supported.
- A request has **60 seconds** to start responding before Compute cancels it.
- Work that continues after the response, such as processing a webhook after returning `202`, uses `waitUntil`. That keeps the instance alive but does not retry a failure or survive a redeploy.

Compute does not require Prisma ORM, so an app on ORM 6 runs unchanged. The migration behavior above assumes ORM 8.

Netlify Functions run on **Node.js**, and the edge network, caching, and Image CDN sit in front of the site without any application code. For asynchronous work, [Background Functions](https://docs.netlify.com/build/functions/background-functions/) run for up to 15 minutes, and [Async Workloads](https://docs.netlify.com/build/async-workloads/overview/) add retries and multi-step jobs. If your app already has a queue-and-worker shape, Netlify supports it directly. On Compute, that work has to move into short requests or a Composer cron schedule.

## What you pay for

Prisma Compute charges a plan fee plus four usage meters. Memory is billed while an instance is running and nothing is billed once it has scaled to zero, so an idle preview costs nothing. Deployments, preview branches, and seats are not billed.

Netlify bills metered usage in [credits](https://docs.netlify.com/manage/accounts-and-billing/billing/billing-for-credit-based-plans/credit-based-pricing-plans/) from a monthly allowance. Production deploys consume credits, while Deploy Previews and branch deploys do not. The dollar column below converts credits at the Pro rate of $20 for 3,000 credits, which is the same per-credit price as Netlify's Pro add-on.

| Meter | Prisma Compute | Netlify (credits, in dollars at the Pro rate) |
| --- | --- | --- |
| Plan | Free, or $10, $49, or $129 a month with 5, 20, or 100 million requests included | Pro from $20 a month for 3,000 credits |
| Requests | $1 per million after the included amount | 2 credits per 10,000, about $1.33 per million |
| Compute | $0.006 per GB-hour of memory plus $0.064 per vCPU-hour of CPU | 10 credits per GB-hour, about $0.067, with no separate CPU meter |
| Bandwidth | $0.025 per GB | 20 credits per GB, about $0.13 |
| Database compute | Prisma Postgres, priced separately | 10 credits per GB-hour, about $0.067 |
| Production deploys | Not billed | 15 credits each, about $0.10 |
| Preview and branch deploys | Not billed | Not metered |

Prisma Postgres has [its own pricing](https://www.prisma.io/pricing). Netlify Database's compute and bandwidth come out of the same credit allowance as the site. One exception to scale-to-zero on Prisma: Composer's cron scheduler keeps one instance awake per deployed app or stage so that it can fire on time.

## Using Prisma ORM and Prisma Postgres on Netlify

Prisma ORM does not require Prisma's hosting. Netlify Database is Postgres, so the ORM connects to it like any other Postgres database, and the [tooling page](https://docs.netlify.com/build/data-and-storage/netlify-database/tooling/) confirms that any driver or ORM works.

To run migrations, add `npx prisma@latest db migrate --db "$NETLIFY_DB_URL"` to your build or release step. Netlify exposes each preview's database branch through that variable. Leave Netlify's own migrations feature unused for that database.

Prisma Postgres is also available to Netlify sites through the official Netlify extension. The [Netlify guide](https://www.prisma.io/docs/guides/postgres/netlify) walks through it and is written for Prisma ORM 7 today.

## Frequently asked questions

<Accordions type="single">
  <Accordion title="Is Prisma a replacement for Netlify?">
For a TypeScript app, mostly. Prisma Compute hosts the app and Prisma Postgres holds the data. It does not replace Netlify's edge network, caching, and Image CDN, and Compute has no WebSockets, no multi-region deployment, and a 60-second limit before a request must start responding.
  </Accordion>
  <Accordion title="Does Prisma give each preview its own database?">
Yes, if you create it. Each Git branch is an isolated environment, but you provision the database with the CLI or a Composer stage deploy, and it starts without production data. A preview-scoped or branch-scoped `DATABASE_URL` keeps the preview off production.
  </Accordion>
  <Accordion title="What is the migration graph in Prisma ORM 8?">
A record of which schema version each migration starts from and produces. After two branches merge, the ORM can plan a path from each database's current version to the merged schema. It is an ORM feature and works on any host, including Netlify.
  </Accordion>
  <Accordion title="Can I use Prisma ORM with Netlify Database?">
Yes. Netlify Database is PostgreSQL built on Neon, and Prisma ORM connects to it like any other PostgreSQL database. Run migrations in your own build or release step. Prisma Postgres is also available through the official Netlify extension.
  </Accordion>
</Accordions>

## Which one to pick

**Pick Prisma** if your backend is several TypeScript services that share Postgres and you can live with Bun, one region, and pre-release tooling. The compiler then checks the wiring between those services, and the deploy creates it. Start with the [Composer getting-started guide](https://www.prisma.io/docs/composer/getting-started), which runs two services locally without an account.

**Stay on Netlify** if your app is a site with functions, needs Node.js, long background jobs, or a global edge, or if you want previews that arrive with production data. The [Netlify guide](https://www.prisma.io/docs/guides/postgres/netlify) brings Prisma ORM 8 and Prisma Postgres there.
