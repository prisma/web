---
title: Supported database features
description: 'The contract records which database features your packages support, so Prisma ORM can reject an unsupported one early with a clear error.'
metaTitle: Supported database features in Prisma ORM
metaDescription: 'How a Prisma ORM contract records which database features you can use, where the list comes from, what each key controls, and what happens when one is missing.'
url: /orm/contract-authoring/capabilities
---

Your [contract](/orm/contract-authoring/the-data-contract), the `contract.prisma` file that replaced `schema.prisma`, describes your data. [`npx prisma contract emit`](/cli/contract-emit) writes `contract.json` and `contract.d.ts` beside it. The `capabilities` section of `contract.json` lists the database features you can use, under keys such as `sql.lateral`. Prisma ORM writes that section, and you never edit it. A feature that is not listed fails right away, with an error that names its key, instead of reaching the database and failing there.

## Where the `capabilities` section comes from [#where-capabilities-come-from]

The keys come from your database package (`@prisma/orm-postgres`, `@prisma/orm-sqlite`, or `@prisma/orm-mongo`) and any extension packages listed in `prisma.config.ts`. `npx prisma contract emit` never connects to your database. It reads those packages and writes what they report, in groups:

```json title="src/prisma/contract.json (excerpt)"
{
  "capabilities": {
    "postgres": { "distinctOn": true, "pgvector.cosine": true },
    "sql": { "lateral": true, "scalarList": true }
  }
}
```

A key is written group first, so `pgvector.cosine` under `postgres` is `postgres.pgvector.cosine`. The `sql` group holds keys that more than one SQL database may support. SQLite reports `false` for most of them. Keys under `postgres` belong to PostgreSQL only. Extension packages add keys of their own, and `pgvector.cosine` comes from the pgvector package. Install it with `npm install @prisma/orm-extension-pgvector`, list it in `prisma.config.ts`, and run `npx prisma contract emit` again:

```typescript title="prisma.config.ts"
import { definePrismaConfig } from "prisma/config";
import pgvector from "@prisma/orm-extension-pgvector/control";
import { defineConfig as ormConfig } from "@prisma/orm-postgres/config";

export default definePrismaConfig({
  orm: ormConfig({
    contract: "./src/prisma/contract.prisma",
    extensions: [pgvector],
    db: { connection: process.env['DATABASE_URL']! },
  }),
});
```

pgvector is also a PostgreSQL server extension, and the first migration the package ships installs it in PostgreSQL for you.

## What the `capabilities` section controls [#what-capabilities-gate]

**When the contract is built.** If your contract uses a feature your database cannot store, `npx prisma contract emit` fails. SQLite has no `scalarList` key, so a list field such as `tags String[]` fails on SQLite:

```text
Field "User.tags" is a scalar list, but target "sqlite" does not support
scalar lists (the adapter does not report the "scalarList" capability).
Remove the list or author it against a target that supports scalar lists.
```

In that message, "target" and "adapter" both mean your database package, which is `@prisma/orm-sqlite` here. Do one of the two: remove the list field, or move to a database that supports list fields. To move to PostgreSQL, change the import and the connection string in `prisma.config.ts`, then set up the new database.

**When you call a method on the [SQL query builder](/orm/reference/sql-query-builder).** A method that needs a feature throws an error whose `code` is `ORM.CAPABILITY_MISSING`. The message names the method and the key:

```text
distinctOn() requires capability postgres.distinctOn
```

That message means the method is not available on the database you are connected to. Rewrite the query without it, or move to a database that supports the feature. If the key comes from an extension package, install that package, list it in `prisma.config.ts`, and run `npx prisma contract emit` again. The error arrives when you call the method, not when you `await` the query, so a test that calls it catches it. Your own code can read the same list through `db`, the client you create once in `src/prisma/db.ts`. A value such as `db.contract.capabilities.sql?.lateral` is `true`, `false`, or `undefined` when the whole group is absent:

```typescript
if (db.contract.capabilities.sql?.lateral) {
  // run the lateral join here, or fall back to a second query
}
```

## Example keys [#example-capabilities]

A few of the keys your database package and extension packages supply:

| Key | What it covers |
| --- | --- |
| `sql.lateral` | Lateral joins (`lateralJoin()`) |
| `sql.returning` | `RETURNING` clauses on writes |
| `sql.scalarList` | List fields such as `tags String[]` |
| `postgres.distinctOn` | `DISTINCT ON` queries (`distinctOn()`) |
| `postgres.pgvector.cosine` | Cosine distance from the pgvector package |

PostgreSQL reports `true` for all three `sql` keys above, and SQLite reports `false` for them. This table is a sample, not the full list. Open `src/prisma/contract.json` in your project to see every key you have. The check that runs when the contract is built applies on every database, MongoDB included. The keys that methods check at query time exist only on the SQL query builder, which MongoDB does not have. The [ORM reference](/orm/reference) lists the methods each database has.

## The `capabilities` section and `prisma db verify` [#capabilities-and-verification]

The keys say what your packages support, recorded when you run `npx prisma contract emit`, so the same contract behaves the same way in every environment. Checking the live database is a separate job, and [`db verify`](/cli/db-verify) compares the database against your contract. Extension packages are the main source of keys beyond the ones your database package brings, and [using extensions](/orm/extensions/using-extensions) shows the full path from install to first query.

## Prompt your coding agent

Projects created with `npm create prisma@latest` include the [Prisma ORM skills](/ai/tools/skills#available-skills-for-prisma-8) for your coding agent. In an existing project, run `npx prisma skills sync`. The `prisma-8` skill covers this material. Ask your agent to:

- "Which keys in the `capabilities` section does our code rely on, and which package provides each?"
- "Add pgvector to the project and confirm its key shows up in the emitted contract."

## Next steps

- See where the `capabilities` block is, and what `contract.d.ts` holds, in [contract.json and contract.d.ts](/orm/contract-authoring/the-contract-artifact).
- Add extension packages, and the keys they bring, in the [CLI configuration](/cli/configuration).
