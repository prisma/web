---
title: Release status
description: 'Where Prisma ORM 8 stands today, what you get when you install it, and how to stay on Prisma ORM 7.'
url: /prisma-orm/release-status
metaTitle: Prisma ORM release status
metaDescription: 'Prisma ORM 8 is a release candidate, with general availability expected in October 2026. What npm installs today, how long Prisma ORM 7 is supported, and how to stay on version 7.'
---

Prisma ORM 8 is a **release candidate**. You can install it and build with it today. Until the final release, some details of the API may still change between release candidates, and each release candidate fixes bugs found in the previous one.

## When the final release is expected

General availability is expected in October 2026.

## What happens to Prisma ORM 7

Prisma ORM 7 keeps receiving bug fixes and security updates for 18 months from the day Prisma ORM 8 reaches general availability. Its documentation stays at [/orm/v7](/orm/v7).

## What you get when you install today

Installing Prisma ORM 8 gives you two things with two different version numbers:

| You install | You get | Version today |
| --- | --- | --- |
| `prisma` | the command-line tool | `8.0.0-rc.13` |
| `@prisma/orm-postgres` | the library your app imports | `8.0.0-rc.9` |

The two numbers are not supposed to match. The command-line tool and the library are released separately, and the tool gets more frequent releases. Versions above were checked on 11 September 2026; run `npm view prisma version` for today's.

`@prisma/client`, the library that Prisma ORM 7 apps import, stays at `7.10.0`. It belongs to Prisma ORM 7 and is not part of Prisma ORM 8.

:::warning[If you have a Prisma ORM 7 project]

`npm install prisma` and `npx prisma` now give you the Prisma ORM 8 command-line tool. It does not read `schema.prisma` and has no `generate`, `migrate dev`, or `db push` commands. If your project is on Prisma ORM 7, lock the version as shown below before your next install.

:::

## How to stay on Prisma ORM 7

Lock both packages to version 7 in `package.json`:

```json title="package.json"
{
  "devDependencies": {
    "prisma": "^7"
  },
  "dependencies": {
    "@prisma/client": "^7"
  }
}
```

Anywhere you run the tool without a locked version, for example in CI or in a one-off command, write `npx prisma@7` instead of `npx prisma`.

The Prisma ORM 7 documentation is at [/orm/v7](/orm/v7), its setup guides at [/v7/getting-started](/v7/getting-started), and its command reference at [/cli/v7](/cli/v7).

## How to try Prisma ORM 8 in an existing app

You do not have to switch all at once. Both versions can run in the same application against the same database, so you can move one part of your app at a time.

- [Migrate from Prisma ORM 7 to 8](/guides/upgrade-prisma-orm/postgresql) walks through that step by step on PostgreSQL.
- [Coming from Prisma ORM 7](/orm/coming-from-prisma-orm-7) shows what each Prisma ORM 7 command and query is called now, and which features are not available yet.
