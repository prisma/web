---
title: migration status
description: Show the Prisma 8 migration path and pending status.
url: /cli/v8/migration-status
metaTitle: migration status | Prisma 8 CLI
metaDescription: Learn how to inspect the Prisma 8 migration path and pending migrations.
---

`migration status` shows which migrations are pending between the database marker and the target contract.

Use it before and after [`migrate`](/cli/v8/migration-apply), and when debugging why an environment is not at the expected contract state.

## Usage

```npm
npx @prisma/cli@next migration status --db "$DATABASE_URL"
```

## Options

| Option | What it does |
| --- | --- |
| `--db <url>` | Connects to the database. |
| `--space <id>` | Narrows output to a single contract space. |
| `--to <contract>` | Sets the target contract reference (hash, prefix, ref name, migration directory name, `<dir>^`, or `./path`). |
| `--from <contract>` | Sets the origin contract reference. Supplying it switches to offline path computation, with no database needed. |
| `--legend` | Prints a key for the tree glyphs and lane colors. |
| `--ascii` | Uses ASCII glyphs (pipe-friendly). |
| `--config <path>` | Reads a specific `prisma-next.config.ts` file. |
| `--json` | Prints a machine-readable result. |

## Examples

```npm
npx @prisma/cli@next migration status --db "$DATABASE_URL"
npx @prisma/cli@next migration status --to production
npx @prisma/cli@next migration status --from abc123 --to production
npx @prisma/cli@next migration status --ascii
```

## Reading the result

With `--db`, status compares the on-disk migration packages to what has been applied in the database. With `--from`, it computes the path offline instead, without a database.

The `migration` group has three more read-only views: `migration graph` for topology, `migration log` for executed history, and `migration list` for on-disk enumeration. Run each with `--help` for details.

Use [`db verify`](/cli/v8/db-verify) after applying migrations to check the final database shape against the emitted contract.
